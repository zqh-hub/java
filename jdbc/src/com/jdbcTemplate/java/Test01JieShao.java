package com.jdbcTemplate.java;
/*
* Spring JDBC：Spring 框架对JDBC的简单封装，提供了一个JDBCTemplate对象简化JDBC的开发
* 步骤：
*   > 导入jar包
*   > 创建jdbcTemplate对象，依赖数据源DataSource
*      new JdbcTemplate(ds);
*   > 调用JdbcTemplate的方法来完成CRUD的操作
*       update():执行DML,增、删、改
*       queryForMap():查询结果将结果集封装为Map集合
*       queryForList():查询结果将结果集封装为List集合
*       query():查询结果将结果集封装为javaBean集合
*       queryForObject()：查询结果将结果集封装为对象
* */
public class Test01JieShao {
}
